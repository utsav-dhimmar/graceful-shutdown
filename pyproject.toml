[project]
name = "graceful-shutdown"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "utsav-dhimmar", email = "utsavdhimmar987@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.141.1",
]

[tool.uv]
package = false
